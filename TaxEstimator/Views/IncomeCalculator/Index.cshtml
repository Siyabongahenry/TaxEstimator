@model TaxEstimator.Models.PaySlip;
@{
    ViewData["Title"] = "Income Page";
}
<div class="text-center">
    <div class="row">
        <div class="col-12 col-lg-8">
            <div class="d-inline-block p-2 bg-white pb-4" style="box-shadow:2px 2px 2px #f3f3f3,-2px -2px 2px #f3f3f3;">
                <h4 class="text-white p-2 shadow" style="background-color:var(--theme-color);">Calculate Your Take Home Income(R)</h4>
                
                @await Html.PartialAsync("_IncomePartial.cshtml",Model.Income)
               
            </div>
        </div>
        <div class="col-12 col-lg-4">
           
                @await Html.PartialAsync("_PaySlipPartial.cshtml", Model)
           
            <div class="text-center">
                <button class="btn btn-secondary text-white rounded-0" onclick="printElem('takeHomeIncome')">Print document</button>
            </div>
        </div>
    </div>
</div>